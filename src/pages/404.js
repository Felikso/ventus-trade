import React from "react";
import { Layout, SEO } from "components/common";

const noFound = () => {

	return (
		<Layout>

		<SEO />
        <h2>404 error</h2>

		</Layout>
	)
};

export default noFound;
